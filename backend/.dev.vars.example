# Copy to .dev.vars for local `wrangler dev` (never commit the real .dev.vars file)
# Generate JWT_SECRET with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
RESEND_API_KEY=
