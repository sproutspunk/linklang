# Set in Cloudflare Pages project settings (Settings → Environment variables),
# not committed here — point this at your deployed Worker URL.
VITE_API_URL=https://linklang-api.<your-subdomain>.workers.dev
